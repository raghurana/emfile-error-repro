{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "ulimit -n 1024 && tsx ./src/index.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "chmod +x ./scripts/deploy.sh && ./scripts/deploy.sh",
    "predeploy": "npm run typecheck",
    "seedq": "node ./scripts/sqs-load.js"
  },
  "keywords": [],
  "author": "Raghu Rana",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@aws-sdk/client-sqs": "^3.967.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "esbuild": "^0.27.2",
    "tsx": "^4.21.0"
  }
}