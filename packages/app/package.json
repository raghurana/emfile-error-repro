{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "chmod +x ./scripts/deploy.sh && ./scripts/deploy.sh",
    "predeploy": "npm run typecheck",
    "seedq": "chmod +x ./scripts/sqs-load.sh && ./scripts/sqs-load.sh"
  },
  "keywords": [],
  "author": "Raghu Rana",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "esbuild": "^0.27.2"
  }
}
